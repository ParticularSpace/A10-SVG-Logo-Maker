const { Triangle, Circle, Square } = require("./shapes");

class LogoMaker {
  constructor(text, textColor, shapeType, shapeColor) {
    // Initialize the properties based on the user input
  }

  createShape() {
    // Create the appropriate shape instance based on the user input
  }

  renderShape() {
    // Render the SVG markup for the selected shape
  }

  addTextToSVG() {
    // Add the text to the SVG markup with the appropriate color
  }

  generateLogo() {
    // Generate the complete SVG markup by combining the shape and text
  }
}

module.exports = LogoGenerator;
